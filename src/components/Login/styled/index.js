import styled from "styled-components";

const LoginStyle = styled.div`
 .container {
    text-align:center;
    


 .icon{height:10px};

.mouse:hover{
    color:${(props) => props.theme.Colors.hover};
     cursor: pointer;
     } 
     
 }


`
export const data = {
    LoginStyle,
}
